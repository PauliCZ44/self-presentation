---
// props of title

export interface Props {
	title: string
}

const { title } = Astro.props
---

<div class="relative">
	<h3 class="text-slate-300 fs-xl">{title}</h3>
</div>

<style>
	h3 {
		text-align: center;
		font-weight: 400;
		font-size: 6.2rem;
		border-radius: 2rem;
		padding: 0.4em;
		/* transition: transform 0.125s ease;
		transform: translateY(calc(3rem + var(--scrollY) * -0.1px)); */
	}
</style>
